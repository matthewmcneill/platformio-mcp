---
description: Kill serial monitor, build, flash, and monitor the attached device
---

Run the safe flash script which kills any existing serial monitor processes to free the port, builds the firmware and flashes it to the currently attached device, and automatically coordinates with the background logging monitor.

// turbo-all
```bash
./.agents/skills/hardware-testing/scripts/safe-flash.sh
```
